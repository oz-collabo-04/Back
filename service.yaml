apiVersion: v1
kind: Service
metadata:
  name: oz-collabo-service
  annotations:
    service.beta.kubernetes.io/ncloud-load-balancer-layer-type: "nplb"
    service.beta.kubernetes.io/ncloud-load-balancer-size: "SMALL"
spec:
  selector:
    app: oz-collabo
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000